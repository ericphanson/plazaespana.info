# Madrid Events - Environment Configuration
# This file is tracked in git and should only contain shared/safe defaults

# Source local configuration if it exists
# Create .envrc.local (gitignored) for your personal settings
if [ -f .envrc.local ]; then
    source_env .envrc.local
fi
